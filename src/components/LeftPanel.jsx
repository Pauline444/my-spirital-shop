

export default function LeftPanel({ title }) {
    return (
        <div className="left-panel">
            <h2 className="m-5 lead">{title}</h2>
            {/* Additional content for the LeftPanel can be added here */}
        </div>
    )
}